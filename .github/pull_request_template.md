<!-- Short imperative summary of the change in the PR title. -->

## What this PR does

<!-- What problem / bug / feature — keep it to a paragraph. -->

## Why

<!-- Context that isn't obvious from the diff. Link to an issue
     if there is one. -->

## Testing

- [ ] `uv run pytest` passes.
- [ ] `uv run ruff check .` passes.
- [ ] `uv run mypy src` passes.
- [ ] Added / updated tests for the fixed bug or new behaviour.
- [ ] For UI changes: manually exercised the dialog / flow and
      noted which.
- [ ] For toolpath / post changes: loaded generated G-code into a
      simulator (CAMotics, UCCNC backplot, your controller's
      preview) and confirmed it matches intent.

## Notes

<!-- Anything reviewers should pay particular attention to — an
     edge case, a concurrent change, a known limitation. -->
